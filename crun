rm core 2>/dev/null

FIL=$1
shift

[[ -f $FIL ]] && FIL=$FIL
[[ -f $FIL.c ]] && FIL=$FIL.c
[[ -f Play/$FIL ]] && FIL=Play/$FIL
[[ -f Play/$FIL.c ]] && FIL=Play/$FIL.c

echo "FIL=$FIL"

[[ -f $FIL ]] && cc -DMAIN -g $FIL && ./a.out $@
stty sane



